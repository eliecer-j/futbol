# render.yaml
services:
  - type: cron
    name: eventos-scraper
    runtime: docker
    plan: starter
    schedule: "0 */6 * * *"  # Ejecutar cada 6 horas
    buildCommand: ""
    envVars:
      - key: TZ
        value: America/Bogota
